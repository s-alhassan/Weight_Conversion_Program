# Weight_Conversion_Program
A simple Python program that converts weights between kilograms (kg) and pounds (lb). The program validates user input, handles invalid entries gracefully, and displays the converted value with two decimal places.
